name: Neo Fetch
on:
  push:
  workflow_dispatch:

jobs:
  neo-fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Fetch Neo
        run: |
          sudo apt-get update
          sudo apt-get install -y neofetch
          neofetch